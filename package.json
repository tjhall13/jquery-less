{
  "name": "jquery-less",
  "version": "0.1.0",
  "description": "jQuery plugin to allow less syntax in place of css for parameterized styles.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "jquery",
    "jquery-plugin",
    "ecosystem:jquery",
    "less"
  ],
  "author": "Trevor Hall",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-jshint": "^0.11.3",
    "grunt-contrib-uglify": "^0.9.2"
  }
}
